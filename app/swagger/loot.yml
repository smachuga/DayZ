get:
  tags:
      - "Loot"
  summary: "A list of loot types"
  description: "Read a list of loot types"
  responses:
    default:
      description: List of Loot Objects
      schema:
        type: array
        items:
          $ref: './loot_def.yml'
