get:
  tags:
    - "Tiers"
  summary: "A list of loot tiers"
  description: "Read a list of loot tiers"
  responses:
    default:
      description: List of Loot Tiers
      schema:
        type: array
        items:
          type: string